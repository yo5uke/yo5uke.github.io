{
  "name": "yo5uke.github.io",
  "build": { "dockerfile": "Dockerfile" },
  "remoteUser": "rstudio",
  "containerEnv": {
    "TZ": "Asia/Tokyo",
    "RENV_CONFIG_REPOS_OVERRIDE": "https://packagemanager.posit.co/cran/__linux__/noble/latest"
  },
  "features": {
    "ghcr.io/rocker-org/devcontainer-features/quarto-cli:latest": {
      "version": "1.8.24"
    }
  },
  "postCreateCommand": "cd ${containerWorkspaceFolder} && python3 -m venv .venv && ${containerWorkspaceFolder}/.venv/bin/python -m pip install -U pip && R -q -e \"install.packages('renv')\" && fc-cache -fv || true",
  "customizations": {
    "vscode": {
      "extensions": ["James-Yu.latex-workshop"]
    }
  }
}
